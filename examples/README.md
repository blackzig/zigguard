# Examples

This directory will contain complete example policies and generated output fixtures for representative stacks.

Planned examples:

- Java + Spring Boot + Oracle
- PHP + Laravel
- Node.js / TypeScript
- legacy application with conservative change policies

Examples should be usable as documentation and as deterministic compiler test fixtures.
